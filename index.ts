const name = "sik2",
    jobs = "programmer",
    gender = "male"

const getInfo = (name, jobs, gender)=> {
    console.log(`Hello ${name}, you are ${jobs}, you are a ${gender}`);
};

getInfo(name, jobs, gender);

export {};